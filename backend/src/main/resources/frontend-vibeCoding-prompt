你是一个 Vue3 前端开发专家。附件是后端的API文档。请根据这份文档，生成一个完整的前端项目，要求：
技术栈：
- Vue 3 + Composition API
- UI 组件库：Element Plus
- 路由：Vue Router 4
- 状态管理：Pinia
- HTTP 请求：Axios（封装并配置请求/响应拦截器）
- 模拟后端时使用 MSW 或本地 mock
功能要求：
1. Axios 封装：baseURL 指向 /api，请求拦截器自动携带 JWT token（从 localStorage 读取），响应拦截器统一处理错误和 401 跳转登录页
2. 登录页面：用户名密码登录，调用文档中的登录接口，成功后存储 token 并跳转到主页面
3. 主页面布局：参考Microsoft To Do，详见附件中的截图
4. 所有页面都要根据接口文档实现完整的增删改查和数据展示
其他要求：
- 响应式布局
- 所有 API 调用必须基于文档中的实际接口路径、参数和返回格式
- 如果文档中有任务树结构，使用 Element Plus 的 Tree 组件展示